import { ark } from "@ark-ui/react/factory";
import { styled } from "styled-system/jsx";
import NextImage from "next/image";

export const Html = styled(ark.html);
export const Body = styled(ark.body);
export const Div = styled(ark.div);
export const Flex = styled(ark.div, { base: { display: "flex" } });
